name: Tests

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8]
    steps:
      - uses: actions/checkout@v2
      - uses: goanpeca/setup-miniconda@v1
        with:
          auto-update-conda: true
          python-version: ${{ matrix.python-version }}
      - name: Install Deps
        run: |
          pip install jupyterlab pandas scipy conda-forge scikit-learn plotly seaborn numpy matplotlib ipywidgets fbprophet
      - name: Get Latest Data
        run: python ./scripts/covid_19_data.py


# https://github.com/ad-m/github-push-action
        

